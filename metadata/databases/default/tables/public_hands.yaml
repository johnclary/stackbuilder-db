table:
  name: hands
  schema: public
insert_permissions:
  - role: user
    permission:
      check:
        user_id:
          _eq: X-Hasura-User-Id
      set:
        user_id: x-hasura-user-id
      columns:
        - hand
        - session_id
        - is_draft
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - hand
        - session_id
        - user_id
        - created_date
        - is_draft
        - modified_date
      filter:
        user_id:
          _eq: X-Hasura-User-Id
update_permissions:
  - role: user
    permission:
      columns:
        - hand
        - is_draft
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      check: {}
